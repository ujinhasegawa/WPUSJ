%h1 My Page
- if user_signed_in?
  ログインユーザ
  = succeed "." do
    %strong= current_user.information.name
  = link_to "ユーザ設定", edit_user_registration_path, :class => "navbar-link"
  |
  \#{link_to "ログアウト", destroy_user_session_path, method: :delete, :class => "navbar-link"}
- else
  = link_to "ユーザ登録", new_user_registration_path, :class => 'navbar-link'
  |
  \#{link_to "ログイン", new_user_session_path, :class => 'navbar-link'}

- if user_signed_in?
  = render 'shared/user_info'

  = render 'shared/user_timeline'
